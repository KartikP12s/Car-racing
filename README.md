# Setup virtual enviornment and download the libraries below by running the command below"
# run "pip install gymnasium[box2d] torch torchvision opencv-python imageio imageio-ffmpeg numpy"
# pip uninstall opencv-python
# pip install opencv-python-headless
